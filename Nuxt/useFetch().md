`useFetch()` — это **композиционный хук в Nuxt 3**, который используется для получения данных (fetch-запросов) как на сервере, так и на клиенте. Он встроен в Nuxt и основан на [`ofetch`](https://github.com/unjs/ofetch).

### Основные моменты:

1. 🔹 **Автоматическая работа на сервере и клиенте**
    
    - Если страница загружается впервые (SSR) — запрос выполнится на сервере.
        
    - Если навигация внутри SPA — запрос выполнится на клиенте.
        
2. 🔹 **Реактивность**  
    Возвращает реактивные значения:
    
    - `data` — результат запроса (реактивный `ref`)
        
    - `pending` — статус загрузки (`true/false`)
        
    - `error` — ошибка, если произошла
        
    - `refresh()` — метод для повторного запроса
        
3. 🔹 **Синтаксис**
    
    `const { data, pending, error, refresh } = await useFetch('/api/users')`
    
    Здесь:
    
    - `data.value` будет содержать результат.
        
    - `pending.value` покажет, идет ли загрузка.
        
    - `error.value` вернет ошибку, если API упал.
        
4. 🔹 **С поддержкой опций**
    
    `const { data } = await useFetch('/api/user', {   method: 'POST',   body: { name: 'Alex' } })`
    
5. 🔹 **В отличие от `fetch()` в JS**
    
    - `fetch()` — это просто браузерный API (всегда работает на клиенте).
        
    - `useFetch()` — обертка от Nuxt, которая:
        
        - учитывает SSR,
            
        - автоматически кеширует данные,
            
        - возвращает реактивные ссылки.